# frozen_string_literal: true

require_relative '../../../config/routes'

RSpec.describe RailsSetLocale::LocaleController, type: :controller do
  it 'set locale to one supported' do
    # get :set_locale, params: {locale: :el}
  end
end
